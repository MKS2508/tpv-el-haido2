# Docker Environment Variables Example
# Copy to .env for local development: cp .env.docker.example .env

# ===========================================
# LICENSE SERVER
# ===========================================

# REQUIRED: Admin token for protected endpoints
# Generate a secure token: openssl rand -hex 32
ADMIN_TOKEN=change-me-in-production

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# ===========================================
# HAIDODOCS (Optional)
# ===========================================

# Base path for subdirectory deployment (e.g., /docs)
# Leave empty for root deployment
# BASE_PATH=

# Enable trailing slashes in URLs
# TRAILING_SLASH=false

# ===========================================
# COOLIFY SPECIFIC (for production)
# ===========================================

# Set these in Coolify's environment variables UI:
# - ADMIN_TOKEN (secret)
# - Custom domains configured via Coolify proxy

# ===========================================
# FUTURE: AEAT BRIDGE (Phase 2)
# ===========================================

# AEAT_PORT=3001
# AEAT_SOAP_ENDPOINT_TEST=https://prewww1.aeat.es/...
# AEAT_SOAP_ENDPOINT_PROD=https://www1.aeat.es/...
# PFX_PATH=/app/certs/certificate.pfx
# PFX_PASSWORD=your-certificate-password
# CORS_ORIGIN=https://your-app-domain.com
