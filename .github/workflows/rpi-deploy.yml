name: 🍓 RPi ARM64 Deploy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-rpi:
    name: 🏗️ Build for Raspberry Pi 3 (ARM64)  
    runs-on: ubuntu-latest-arm64
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4

    - name: 🐳 Setup native ARM64 build environment
      run: |
        # Install basic tools and development dependencies
        sudo apt-get update
        sudo apt-get install -y curl wget build-essential imagemagick pkg-config \
          libglib2.0-dev libssl-dev
        
        echo "✅ Native ARM64 build environment configured"

    - name: 🦀 Install Rust toolchain
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true
        components: rustfmt, clippy

    - name: ⚡ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: 🔧 Install Node dependencies
      run: npm ci

    - name: 🍓 Build complete Tauri application natively
      run: |
        echo "🚀 Building complete Tauri application natively on ARM64..."
        
        # Install Tauri CLI
        npm install -g @tauri-apps/cli
        
        # Build everything with Tauri CLI (includes frontend and backend)
        npx tauri build
        
        echo "✅ Complete Tauri build completed successfully"

    - name: 🔧 Setup Bun runtime
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: 📋 Generate documentation
      run: |
        echo "📚 Generating comprehensive documentation..."
        bun run generate-docs.ts
        echo "✅ Documentation generated"

    - name: 📊 Check generated files
      run: |
        echo "📊 Verifying generated files..."
        
        # Check executable
        if [ -f "src-tauri/target/release/tpv-el-haido" ]; then
          echo "✅ Executable found: $(ls -lh src-tauri/target/release/tpv-el-haido)"
          echo "🔍 File info: $(file src-tauri/target/release/tpv-el-haido)"
        else
          echo "❌ Executable not found"
        fi
        
        # Check DEB package
        if [ -f "src-tauri/target/release/bundle/deb"/*.deb ]; then
          echo "✅ DEB package found: $(ls -lh src-tauri/target/release/bundle/deb/*.deb)"
        else
          echo "⚠️ DEB package not found"
        fi
        
        # Check RPM package  
        if [ -f "src-tauri/target/release/bundle/rpm"/*.rpm ]; then
          echo "✅ RPM package found: $(ls -lh src-tauri/target/release/bundle/rpm/*.rpm)"
        else
          echo "⚠️ RPM package not found"
        fi
        
        # Check documentation
        if [ -f "RPi-Build-Documentation.md" ]; then
          echo "✅ Documentation found: $(ls -lh RPi-Build-Documentation.md)"
        else
          echo "❌ Documentation not found"
        fi

    - name: 📦 Upload complete RPi ARM64 build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: rpi-arm64-build
        path: |
          src-tauri/target/release/tpv-el-haido
          src-tauri/target/release/bundle/deb/*.deb
          src-tauri/target/release/bundle/rpm/*.rpm
          src-tauri/target/release/bundle/appimage/
        retention-days: 30

    - name: 📚 Upload documentation and MD files
      uses: actions/upload-artifact@v4
      with:
        name: rpi-documentation
        path: |
          RPi-Build-Documentation.md
          README.md
          releases/
        retention-days: 30

    # Create release with all artifacts on tagged commits
    - name: 🚀 Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: |
          src-tauri/target/release/tpv-el-haido
          src-tauri/target/release/bundle/deb/*.deb
          src-tauri/target/release/bundle/rpm/*.rpm
          RPi-Build-Documentation.md
        body: |
          # 🍓 TPV El Haido - ARM64 Release for Raspberry Pi 3
          
          ## 📦 Installation Options:
          
          ### Option 1: DEB Package (Recommended)
          ```bash
          sudo dpkg -i "TPV El Haido_0.1.0_arm64.deb"
          sudo apt-get install -f  # Fix dependencies if needed
          ```
          
          ### Option 2: Direct Executable
          ```bash
          chmod +x tpv-el-haido
          ./tpv-el-haido
          ```
          
          ## 🎯 Files Included:
          - **tpv-el-haido**: Direct ARM64 executable (portable)
          - **TPV El Haido_0.1.0_arm64.deb**: Debian package (recommended)
          - **TPV El Haido-0.1.0-1.aarch64.rpm**: RPM package
          - **RPi-Build-Documentation.md**: Complete build documentation
          
          ---
          🍓 *Optimized for Raspberry Pi 3 ARM64* | 🤖 *Built with GitHub Actions*
        draft: false
        prerelease: false